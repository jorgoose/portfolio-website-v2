---
import CallToAction from "./CallToAction.astro";
import Icon from "./Icon.astro";
---

<aside>
  <div class="cta-text">
    <h2>Want to get in touch with me?</h2>
    <p class="cta-subtitle">Whether it's about a project, opportunity, or just to say hi — I'd love to hear from you.</p>
  </div>
  <div class="cta-buttons">
    <CallToAction href="mailto:contact.jorgensen@gmail.com">
      Send Me a Message
      <Icon icon="paper-plane-tilt" size="1.2em" />
    </CallToAction>
    <a href="https://calendar.app.google/v4LK3comv61xPtnT6" target="_blank" rel="noopener noreferrer" class="schedule-btn">
      Schedule a Chat
      <Icon icon="calendar" size="1.2em" />
    </a>
    <a href="/resume.pdf" download class="schedule-btn">
      Download Resume
      <Icon icon="file-arrow-down" size="1.2em" />
    </a>
  </div>
</aside>

<style>
  aside {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 3rem;
    border-top: 1px solid var(--gray-800);
    border-bottom: 1px solid var(--gray-800);
    padding: 5rem 1.5rem;
    background-color: var(--gray-999_40);
    box-shadow: var(--shadow-sm);
  }

  .cta-text {
    text-align: center;
  }

  h2 {
    font-size: var(--text-xl);
    max-width: 15ch;
  }

  .cta-subtitle {
    font-size: var(--text-sm);
    color: var(--gray-400);
    margin-top: 0.75rem;
    max-width: 35ch;
  }

  .cta-buttons {
    display: flex;
    flex-direction: column;
    gap: 1rem;
    align-items: center;
  }

  .schedule-btn {
    display: flex;
    place-content: center;
    text-align: center;
    padding: 0.56em 2em;
    gap: 0.8em;
    color: var(--accent-text-over);
    text-decoration: none;
    line-height: 1.1;
    border-radius: 999rem;
    overflow: hidden;
    background: var(--gradient-accent-orange);
    box-shadow: var(--shadow-md);
    white-space: nowrap;
    position: relative;
    font-size: var(--text-lg);
  }

  .schedule-btn::after {
    content: '';
    position: absolute;
    inset: 0;
    pointer-events: none;
    transition: background-color var(--theme-transition);
    mix-blend-mode: overlay;
  }

  .schedule-btn:focus::after,
  .schedule-btn:hover::after {
    background-color: hsla(var(--gray-999-basis), 0.3);
  }

  @media (min-width: 50em) {
    aside {
      padding: 7.5rem;
      flex-direction: row;
      flex-wrap: wrap;
      justify-content: space-between;
    }

    .cta-text {
      text-align: left;
    }

    h2 {
      font-size: var(--text-3xl);
    }

    .cta-subtitle {
      font-size: var(--text-md);
      max-width: 45ch;
    }

    .cta-buttons {
      flex-direction: row;
      gap: 1.5rem;
    }

    .schedule-btn {
      padding: 1.125rem 2.5rem;
      font-size: var(--text-xl);
    }
  }
</style>
