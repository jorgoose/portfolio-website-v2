<div class="pill"><slot /></div>

<style>
	.pill {
		display: flex;
		padding: 0.25rem 1rem;
		gap: 0.5rem;
        margin-right: 0.2rem;
		color: var(--accent-text-overlay);
		border: 1px solid var(--accent-overlay);
		background-color: var(--accent-overlay);
		border-radius: 999rem;
		font-size: var(--text-sm);
		line-height: 1.35;
		white-space: nowrap;
	}
</style>